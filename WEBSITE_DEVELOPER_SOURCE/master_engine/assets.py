"""V5 deliberately provides no music, SFX, stock or placeholder B-roll APIs.

Only the original source media enters the render graph. Older optional asset
files remain recoverable in the installer backup and are never loaded here.
"""
